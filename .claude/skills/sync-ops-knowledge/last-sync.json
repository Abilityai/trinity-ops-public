{
  "last_commit": "93d7ce7c85e071eb4a086c3b653ff56e8cbb11e4",
  "last_sync_date": "2026-08-17",
  "instance_used": "local (~/Dropbox/trinity/trinity — origin/main == v0.9.0 release commit; tree identical to origin/dev c367485e)",
  "baseline_before": "c1cc22bc002cd09bc4b98f374392915880358b30 (previous sync point on dev, 2026-08-14)",
  "commits_reviewed": 86,
  "note": "User arg: 'from main'. main is squash-released (93d7ce7c 'Release: v0.9.0 (#2268)'), so the dev-side last_commit is not its ancestor; trees of main and dev were verified identical, content delta taken as `git diff c1cc22bc origin/main`, commit bodies from dev's granular log, and docs/releases/0.9.0.md read as the operator summary. VERSION 0.8.5 -> 0.9.0. Public-repo policy carried forward: enterprise (ent#) items documented only where they have an OSS ops surface. .env absent in ops-public, so no scripts/run.sh use.",
  "changes_applied": [
    "CLAUDE.md Backup Database: rewritten for automatic backups (#2216) — nightly 03:30 UTC both backends, /data/backups artifact names, boot pre-migration copy, backup_retention_days via PUT /api/settings/ops/config (0 rejected, min-keep 3), DB_BACKUP_ENABLED, _db-backup sentinel alarms + weekly staleness re-alarm, same-disk scope; manual on-demand via sqlite3 .backup (never cp), .env manual backup, SQLite restore sequence (stop backend AND scheduler, rm -wal/-shm/-journal, cp), PG pg_restore pointer",
    "CLAUDE.md Update Trinity: v0.9.0 upgrade notes — start.sh never rebuilds platform images (#1814), GET /api/version version vs image_version, base image rebuild + cold stop/start adoption (#1809/#1860/#1816), running agents never image-recreate",
    "CLAUDE.md API Access: GET /api/canary/status run-state + canary_enabled feature flag (#2217); GET /api/version fields; retention response `backup` block + `blocked_sweeps` (#2146); GET/PUT /api/settings/ops/config incl. backup_retention_days; executions/timeline &split=trigger (ent#96); Session Tab block re-worded as absorbed into Workspace in v0.9.0 (ent#358/#381)",
    "CLAUDE.md Service Architecture: trinity-archives-init row (dev compose only, #2205; prod covered by the start.sh-chowned bind mount); backend row notes postgresql-client-17 major-pinned (#2216); log-rotation block notes archival was dead pre-v0.9.0 and the _log-archive alarm",
    "CLAUDE.md Database Backend: SQLite end-of-support callout (Sept 1 2026, #1278, docs/migrations/SQLITE_TO_POSTGRES.md); Backups line rewritten for automatic both-backend artifacts",
    "CLAUDE.md Security Notes: new 'Admin gates reject agent-scoped principals (ent#297)' subsection (release-notes-surfaced gap — automation using an agent key on admin endpoints must switch to a user key); MCP validator SSRF guard now names CGNAT 100.64.0.0/10 plain + IPv4-mapped (ent#393/#394) in both the validator list and the credential-inject troubleshooting list",
    "CLAUDE.md Troubleshooting: retention entry extended with #2146 refuse-not-raise + blocked_sweeps + retried alarms; 'Long-running task' extended with Workspace turns honoring execution timeout (#2214) and a new 'Fan-out turn recorded SUCCESS but subagent work missing' entry (#2127, AGENT_IDLE_FINALIZE_S, needs base-image rebuild + recreate); 'Rebuilding agents restarted…' gains the #2186 follow-on (start of stopped agent 500'd on builds between #2092 and #2186; v0.9.0 reports recreate_reason); 'Update broke things' points at pre-migration copies + correct restore; three new entries — nightly backup failed/stale (#2216), log archival dead / /data/archives root-owned (#2205) with probe commands and the pre-v0.9.0 manual chown, operator-queue items from _db-backup/_log-archive sentinels + OPERATOR_ALERT_MAX_PENDING_PER_TYPE budget (#1677)",
    "CLAUDE.md Env table: DB_BACKUP_ENABLED/_HOUR/_MINUTE/_PG_DUMP_TIMEOUT_SECONDS (#2216); OPERATOR_ALERT_MAX_PENDING_PER_TYPE (#1677); AGENT_IDLE_FINALIZE_S (#2127); Scripts Reference row for backup.sh; three 'v0.9.0-dev' strings -> 'v0.9.0'",
    "scripts/backup.sh: rewritten — sqlite3 online .backup + PRAGMA quick_check (never a live cp), bind-mount vs volume auto-detect, bundled-PG pg_dump -Fc arm (exit 2 + instructions for external PG), saves to ~/backups/ (was /tmp), prints how to list the platform's /data/backups artifacts",
    "scripts/update.sh: step 1 delegates to backup.sh (aborts on failure); detects docker/base-image/ changes in the pulled range and prints the build-base-image.sh + cold stop/start adoption reminder; verify step prints version vs image_version and warns on stale image; optional checks guarded against set -e",
    "SKILL update: step 5 uses scripts/backup.sh (exit-2 external-PG handling), mentions pre-migration copies; step 8 annotated as load-bearing (#1814); new step 8b base-image change detection + adoption rules (#1809/#1860/#1816, never docker restart) + /rebuild-agent for run-state-preserving waves; step 11 checks /api/version version==image_version and [DBBackup] pre-migration log; summary table gains version/image and base-image rows",
    "SKILL rollback: argument accepts /data/backups/ platform artifacts; step 6 uses scripts/backup.sh + lists platform artifacts; step 8 restore now stops backend AND scheduler, auto-detects data mount, quick_checks the source, removes -wal/-shm/-journal sidecars, cp + chown 1000:1000, PG pointer; step 9 note on base-image across a rollback",
    "SKILL cleanup: prunes .dump as well as .db in ~/backups; explicit do-not-touch for /data/backups (platform-pruned by backup_retention_days) and /data/archives",
    "SKILL migrate-to-postgres: first PG backup switched to pg_dump -Fc .dump (matches the platform's nightly artifact shape); pointer to the automatic job's status block; follow-ups line updated (backup.sh/update PG-aware for bundled PG; rollback restore still SQLite)",
    "SKILL rebuild-agent: intro explains when a cold stop/start suffices vs. this skill (#1809/#1860/#1816, #2186 workaround), and what survives a recreate incl. the committed self-healing .trinity/plugins.yaml manifest (#1704) and the recreate-not-restart env knobs",
    "SKILL status: version step also reads /api/version version vs image_version, summary shows STALE IMAGE",
    "SKILL diagnose: new step 9b 'Silent maintenance-job failures' — mints an admin token on the host, reads retention .backup/blocked_sweeps, lists /data/backups, probes /data/archives writability + sizes, greps [DBBackup]/[ArchiveStorage]/REFUSED; summary gains a Maintenance jobs line",
    "SKILL sync-ops-knowledge: Step 2 records the 'from main after a dev sync' range rule (squash release, trees-equal check, content diff vs main + messages from dev, read docs/releases/<v>.md first, record the main SHA)"
  ],
  "changes_skipped": [
    "Enterprise-entitled / ent#-only items with no OSS ops lever: rooms engine + Workspace multi-agent chats, A2A per-caller inbound allow-list, skill-runner ACLs (ent#219/#241), grid tiles/org overlay UI, portal delegated identity (ent#163) — per the public-repo policy",
    "Workspace UX fixes (#2211/#2212/#2213/#2159/#2160/#2161/#2162/#2169/#2196/#2198/#2258/#2262 sign-out, #2157 portal voice reason codes): client-facing UI/behavior, no operator runbook change; #2214 (turn bound) IS documented",
    "A2A outbound edge sweep (#2174/#2175/#2177/#2180/#2181/#2183, ent#395–#399): correctness fixes to an already-documented surface (delete-one-endpoint, port normalisation, blank credential leaves secret alone) — no new operator action",
    "SSH-port allocation made atomic + first-run seeding pass-level lock (#2215/#2235): behavior fix; the 'partial' starter-fleet latch it cures is pre-v0.9.0 history",
    "Agent-runtime internals: Codex pin 0.147.0 + GPT-5.x pricing (#2207/#2209), model catalog centralization (#2086/#2230), git repo discovery across mount boundary (#2245/#2251), canonical .gitignore merge at creation (#2069/#2226), telemetry+transcript on 504 rows (#1853/#2224), zip uploads (#2152), plugin-manifest details beyond the rebuild-agent note (#1704/#2225)",
    "Refactors with no surface change: SingleFlightLock consolidation (#1920/#2232), slack.py inv8 carve-out retirement (#1710/#2231)",
    "CI/test-suite fixes (#2223/#2227/#2241–#2250 …), frontend dashboard tiles (#2184/#2228/#2237/#2263), client-side cron validation UI (#925/#2167), user-docs refreshes (#2253/#2265): not ops-agent surfaces",
    "Release-note items already documented in the previous sync (Workspace OSS core, template registry, skills multi-source, A2A inbound/outbound, container log caps, MCP key self-heal, env-drift, audit tri-state, CSO #2081): left as-is; only version-string wording refreshed"
  ],
  "reviewed_unchanged": [
    "migrate-to-postgres ETL / schema path: no new tables in this range (backup status lives in system_settings; SSH-port reservation is Redis-only) — skill body untouched apart from the backup lines",
    "restart / logs / telemetry / agents / provision skills: no changed command surface",
    "Redis ACL (#589), AGENT_AUTH_SECRET (#1159), DOCKER_GID detection (#1131), Vector group_add (#1799), healthcheck relaxation (#1230), protected-files list, MCP validator command allowlist: unchanged"
  ]
}
