{
  "last_commit": "eb41896f57587d33ea9ba5714383cd7a9180b652",
  "dev_tip": "eb41896f57587d33ea9ba5714383cd7a9180b652",
  "last_sync_date": "2026-09-14",
  "instance_used": "local (~/Dropbox/trinity/trinity, branch dev; origin/dev eb41896f = VERSION 0.9.5-rc3; origin/main 646c9314 is still the v0.9.0 line + hotfixes/deps)",
  "baseline_before": "93d7ce7c (v0.9.0 release squash on main; tree-identical to dev tip c367485e, which is the true start of this range)",
  "commits_reviewed": 308,
  "note": "User arg: 'from dev'. The recorded last_commit (main squash 93d7ce7c) is an ancestor of dev only via the re-sync merge, so 93d7ce7c..origin/dev listed 743 commits of already-reviewed granular history; the real delta is c367485e..origin/dev (308 commits, 236 after filtering docs/deps/test/ci). Range research fanned out to five parallel subagents by area (deploy, auth/security, agent runtime, platform features, runbook-claim verification); endpoint paths and body shapes they disagreed on were verified against origin/dev in the main context. Public-repo policy carried forward: ent# items documented only where they have an OSS ops surface. .env absent in ops-public, so no scripts/run.sh use. No release doc exists for 0.9.5 yet (rc3); docs/releases/0.9.0.md only gained a marketplace section + commit-reconciled addendum.",
  "changes_applied": [
    "CLAUDE.md Restart Services: never `docker compose down` (#2541 unless-stopped agents + recreated network → dockerd restart loop; roster shows `stopped`); recovery = rm -f + POST start / fleet restart; stop.sh",
    "CLAUDE.md Update Trinity: v0.9.5 upgrade notes — CREDENTIAL_ENCRYPTION_KEY must be set before boot (ent#435/#2330 secret-settings migration deletes plaintext, MissingEncryptionKeyError, rotate Settings-entered creds, re-run rotate-credential-key once if rotated before); mandatory base-image rebuild + cold recreate (#2537 arm64, ent#345 managed-settings, ent#411 plugin, #2333 codex auth, #2503 diagnostics, #2476 deny list, #2398 sanitizer, #2435); one-shot restart-policy sweep with the docker update loop + consequences; PG `alembic heads` (0045 merge); node:26 + shared_sessions + nginx /mcp image changes; #2432 retention rows seeded every boot; hosted upgrade = start.sh --hosted; /api/version install_source + /token 403 change",
    "CLAUDE.md Agent Management: restart-policy census + SIGUSR1 thread-dump commands; 'stopped stays stopped' semantics incl. the emergency-stop error caveat",
    "CLAUDE.md API Access: token block rewritten (ADMIN_USERNAME must match, #2357 403 mfa_required + exclude_none, `.access_token // empty`, 2FA-policy → MCP key path); /api/telemetry/host corrected to AUTHENTICATED (stale since before 0.9.0, SEC-180); ops key scope mint (POST /api/mcp/keys scope=ops) with the exact route fence + owner-bound caveat; audit-log mcp_key_id filter; /api/version install_source + feature-flags posture fields; Session Tab note gains the Workspace-native sessions family; new blocks — operator-resume GET/PUT (#2312), operator-queue respond 422 offered_options (#2376), fan-out poll (#2679) + queued_timeout receipt (#2661), subscription usage/headroom/test/settings endpoints + _sub-headroom sentinel + #2741 adoption wave, api-keys gemini/resend (ent#582) + generic PUT 422, operator-intake + telemetry ask/dismiss + preview fields (#2706), room-budget-defaults (ent#443), canvas list/shares/revoke + CANVAS_MAX_PER_AGENT, systems restart human-only + schedules projection (#2373)",
    "CLAUDE.md Database Operations: new table rows (enterprise_rooms*, agent_canvases/agent_canvas_shares, subscription_headroom_history, portal_file_dismissals, user_ui_preferences) + portal session/message columns; v0.9.5 columns paragraph (operator_resume_enabled, turn_integrity NULL≠healthy, open_canvas_id, source_channel_client, deliver_to_workspace_email fails the run, agent_loops terminal-driven, addressed_to_email, failure_kind, evaluations target/comment redaction); alembic head 0061; 0041 data migration pointer",
    "CLAUDE.md Provisioning: DO one-command path (trinity-do-create.sh, #2707) + droplet facts (/opt/trinity, FRONTEND_PORT 8081, Caddy IP cert, tls-allowed domain gate, ops-agent .env); requirements row (8 GB for --hosted); new 'Install modes: source build vs hosted images (#2280)' section — three standalone stacks table, never-stack rule with the exact Compose failure modes (#2557), start.sh data-store guard (#2390), hosted specifics (start.sh --hosted pulls + retags base image, pin TRINITY_IMAGE_TAG, tag spellings, rc never moves latest #2499, repo checkout still required, tunnel profile, quickstart alias, stop.sh label detection, OSS/amd64 images, publish workflow); unattended note (#2385 printed password works, env ADMIN_PASSWORD written through); `--provision --cloud digitalocean` paragraph; /setup refusal (#2385)",
    "CLAUDE.md Service Architecture: frontend row (FRONTEND_PORT honoured, nginx /mcp proxy #2475, node:26); trinity-cloudflared row (profile-gated); agent row (unless-stopped); 'Restart policies (#2541)' paragraph incl. daemon-restart bypasses the start ladder",
    "CLAUDE.md Database Backend: alembic head 0061 + `alembic heads` check for the 0043/0044 fork",
    "CLAUDE.md Security Notes: four new subsections — 'Machine credentials, attribution, and the ops key scope' (#2323/#2389 allowlist gates, ops fence, owner-bound, X-MCP-Key-* ignored, A2A idempotency key change, X-Source-Agent 403 ent#614, /ws scoping ent#467); 'Secret settings encrypted at rest' (ent#435: rows, boot refusal, key-loss scope, 422 write guard, migration≠remediation, verify SQL); 'Browser admin claim on marketplace images' (ent#580/#2715: race, mitigations, squat detection, forgotten-password recovery, prod :? guard, #2385, email verify allow-list); 'Guardrail hooks live outside the agent's reach' (ent#345: managed-settings.json, GUARDRAILS log lines, no-hooks-on-ERROR); 'Provisioned hosts: firewall and metadata service' (TRINITY-FW chain, 169.254 DROP, no published port off-box, ufw vs iptables-persistent, tls-allowed model). Credential Key Rotation step 2 extended (system_settings pass; re-run once after 0.9.5)",
    "CLAUDE.md Troubleshooting: 'Agent container won't start' gains the compose-down cause; new entries — agents did not come back after reboot (#2541 census + sweep + new restart-count alert); backend refuses to boot MissingEncryptionKeyError (+ 422 secret setting); POST /token 403 mfa_required / 401 ADMIN_USERNAME / MCP-key 403 scope / X-Source-Agent 403; start.sh --hosted failures (manifest unknown vs denied, store guard messages, stacked-compose errors, base image after compose pull, cloudflared profile, /mcp needs new frontend, Caddy/tls-status/tls-allowed, TRINITY-FW outbound); stale base image symptoms (arm64 Exec format error, GUARDRAILS ERROR, Codex 401, sanitizer CPU); wedged agent server (faulthandler, [Diagnostics] lines, kill -USR1 1); executions 503/queued/watchdog (BACKEND_AGENT_CALL_LIMIT parking semantics #2433/#2435, new orphan text, #2752 TIMEOUT vs NETWORK, #2434 duration_ms NULL + PG int4 sweep freeze, QUEUED only for pull pilots); skills source annotated-tag refusal + legacy-adoption items + delivery.status (#2551/#2777/#2764/#2704); operator-resume / approval 422 / subscription LIMIT-vs-auth / #2741 wave / room closed / canvas cap. Fan-out entry gains #2467 turn_integrity + #2476 deny list; sentinel entry gains _sub-headroom, _skills-sync, new id prefixes and item types",
    "CLAUDE.md Env table: ADMIN_PASSWORD (blank only with browser source), ADMIN_USERNAME (prod-forwarded #2385), ADMIN_PASSWORD_SOURCE, CREDENTIAL_ENCRYPTION_KEY (ent#435 scope), TRINITY_IMAGE_TAG, TRINITY_INSTALL_SOURCE, FRONTEND_PORT, COMPOSE_PROFILES, TUNNEL_TOKEN (profile-gated), BACKEND_AGENT_CALL_LIMIT/_QUEUE_TIMEOUT_S, SUBSCRIPTION_SWEEP_CONCURRENCY, MCP_CHAT_TIMEOUT_MS/MCP_RECOVERY_TIMEOUT_MS, CANVAS_MAX_PER_AGENT, WORKSPACE_VOICE_MAX_DURATION, OPERATOR_QUEUE_EMAIL_MAX (folded into the OPERATOR_QUEUE_* row), TRINITY_DEFAULT_SKILL_SOURCE_REF v0.2.0, PORTAL_FILE_* (+ inert PORTAL_RATING_* note), TRINITY_PREINSTALL_PLUGINS/TRINITY_PLATFORM_PLUGINS, LOG_RETENTION_DAYS row gains #2432 seeding + the two subscription retention keys; Scripts Reference update.sh row",
    "scripts/update.sh: rewritten — CREDENTIAL_ENCRYPTION_KEY pre-check (abort, ent#435); hosted-mode detection from COMPOSE_FILE → delegates to `start.sh --hosted --unattended` and warns on unpinned TRINITY_IMAGE_TAG; source mode unchanged (build + up -d, never down); new step 5 one-shot agent restart-policy sweep via docker update; verify block adds ent#435 log grep, `alembic heads` count, hosted base-image adoption reminder, GUARDRAILS hint",
    "SKILL update: step 2 mode detection + hosted TRINITY_IMAGE_TAG pin check; new 2b CREDENTIAL_ENCRYPTION_KEY pre-check; step 8 hosted branch (start.sh --hosted, manifest unknown/denied, store guard) + node:26/shared_sessions note; step 8b adds the 0.9.0→0.9.5 mandatory rebuild list + GUARDRAILS verification loop; new 8c restart-policy sweep + never-down advice; step 9 marked source-only; step 11 adds ent#435 grep + alembic heads; summary table gains Mode / Restart-policy sweep / Secret-settings migration rows",
    "SKILL restart: never-`compose down` paragraph with recovery",
    "SKILL agents: `policy` and `dump` subcommands; restart semantics paragraph (manual-stop flag, docker start bypasses the ladder, pre-0.9.5 containers)",
    "SKILL diagnose: new step 5b (restart-policy census, GUARDRAILS lines, [Diagnostics] events, [InflightDispatch]/watchdog counts) + summary line",
    "SKILL rebuild-agent: restart policy is normalised on recreate; docker update sweep as the cheaper alternative; GUARDRAILS check after the wave",
    "SKILL rollback: rolling back across the ent#435 migration needs the pre-update DB (plaintext rows deleted); hosted rollback = TRINITY_IMAGE_TAG + start.sh --hosted",
    "SKILL provision + provision/digitalocean.md: DO one-command install section (trinity-do-create.sh, what --provision does, droplet facts, ops-agent .env, backend port not reachable off-box); manual path now uses build-base-image.sh + start.sh instead of a bare compose up; ops-agent .env gains COMPOSE_FILE / TRINITY_PATH / FRONTEND_PORT variants; never-stack note",
    "SKILL telemetry: /api/telemetry/host now called with a bearer minted on the host (SEC-180); ops-key mention",
    "SKILL status: /api/version jq adds install_source; hosted-install detection via the compose config_files label + TRINITY_IMAGE_TAG; summary line",
    "SKILL cleanup: hosted GHCR images are the running release; rooms/canvas shares have no sweep by design",
    "SKILL migrate-to-postgres: post-cutover validation adds `alembic heads` (0045 merge)",
    "SKILL sync-ops-knowledge: Step 2 records the 'from dev after a from main sync' range rule (record dev_tip; range from it; tree-equality proof) and the large-range fan-out method (filter, five area subagents, verify disagreements in main context, mechanical route/table/MCP-tool diffs)"
  ],
  "changes_skipped": [
    "Enterprise-entitled / ent#-only with no OSS ops lever: role assignments (#2596 get_agent_assignments is enterprise-backed and inert on OSS), credential vault UI/MCP tools (ent#279 #2264 — 404/403 on OSS; only the audit-visible secret-scrub seam exists), tandem/brief delivery layer (ent#498 beyond the schedule column), fleet-benchmark telemetry read (ent#190), portal delegated identity, canvas design kit/skill (ent#537), room-participant reaper details",
    "Workspace UX (≈60 PRs: rail, tabs, composer, typography, unread badges, voice orb, canvas rendering, Files tab, model dropdown, ratings UI, agent pages, sidebar ordering, stick-to-bottom, code blocks, resizable columns, logo): client-facing, no operator runbook change beyond the tables/columns/env recorded",
    "Frontend dashboard/grid tiles (#2547/#2519/#2536/#2538/#2561 beyond the user_ui_preferences table), skeleton/scanline sweeps, settings field styling (#2490/#2626), brain-orb wheel routing",
    "CI/test-suite work (journey tier + Journey Impact declaration #2335–#2350, nightly verdicts, alembic-head-verdict.js, raw-colour ratchet, submodule-failure classifier, deploy-dev.yml fixes #2622/#2627/#2248, unit-suite timeouts, e2e nightly causes #2714), dev-methodology .claude submodule bumps, code-health dashboards, docs/memory + user-docs refreshes (#2746/#2753/#2758), README video/topology image",
    "Packer marketplace bakery internals (packer/digitalocean/*, mp-submit.sh, listing copy #2680): vendor-side, not a day-two operator command — only the resulting droplet behaviour (browser claim, provenance, firewall) is documented",
    "Agent-runtime internals without an operator lever: execute_task decomposition (#2489), lifespan refactor (#2437), dead-module removal (#2500 — verified nothing the runbook references was removed; db.engine.is_sqlite still exists), Codex/GPT pricing, model catalog (Fable 5.1 selectable #2739), git-sync .gitignore negation fix (#2595), zip uploads, pull-queue loop internals beyond the QUEUED-status note, X-MCP-Key persistence cleanup details, telemetry schema v2 payload fields beyond sharing_id/outcome mix",
    "Portal-facing endpoints not needed by an operator: /api/enterprise/client-portal/asks, /work (pipeline_state), /voice/start, /briefings, uploads/documents delete, ratings, sessions PATCH/main reset, execution terminate (public + portal), /api/public/canvas/{token} beyond the one-line mention, /api/users/me/preferences beyond the table row, /api/onboarding/first-run",
    "Already-on-main hotfixes (#2398/#2406 sanitizer ReDoS, ent#443 rooms move, deps bumps) documented only via their operator consequence (base-image rebuild), not individually"
  ],
  "reviewed_unchanged": [
    "All 21 env vars the runbook relies on are still read by config/services at origin/dev; all runbook endpoints verified present (retention/backup block, canary status, audit verify tri-state, ops config, env-drift, mcp-key, timeline, skills sources, template-registry, manifests, portal-session-policy, a2a-endpoints, soft-deleted recovery, label/circuit-breaker/freeze/bind/connector/reminders/reports/evaluations/compatibility/data export/analytics/files/a2a card/slack dm-default, webhooks, ws ticket); Session Tab endpoints still mounted",
    "Backend Dockerfile still bakes postgresql-client-17; build-base-image.sh and rotate-credential-key.py --apply still exist; install.sh unchanged in range (still a source build; no hosted flag)",
    "Redis ACL (#589), AGENT_AUTH_SECRET (#1159), DOCKER_GID detection (#1131), Vector group_add (#1799), healthcheck relaxation (#1230), protected-files list, MCP validator command allowlist + CGNAT SSRF rule, log-rotation caps, DB backup job (#2216), archives-init (#2205): unchanged",
    "scripts/backup.sh, restart.sh (compose restart — no down), run.sh, status.sh, tunnel.sh: no change needed; logs/telemetry skills only needed the auth fix"
  ]
}
