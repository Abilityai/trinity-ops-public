{
  "last_commit": "6c7e3d27ca32d31fa5023c0fae64771e34f52793",
  "dev_tip": "6c7e3d27ca32d31fa5023c0fae64771e34f52793",
  "last_sync_date": "2026-09-17",
  "instance_used": "local (~/Dropbox/trinity/trinity; checkout on chore/release-0.9.5 at origin/dev 6c7e3d27 with an uncommitted VERSION=0.9.5 + draft docs/releases/0.9.5.md — all code read from origin/dev, VERSION there = 0.9.5-rc5; origin/main 646c9314 still the v0.9.0 line)",
  "baseline_before": "eb41896f (0.9.5-rc3 dev tip)",
  "commits_reviewed": 39,
  "note": "User arg: 'from current dev' + 'learn the DigitalOcean install page' (docs.ability.ai/getting-started/deploying/digitalocean, source trinity-docs@29a6607 app/getting-started/deploying/digitalocean/page.tsx). Small range, no fan-out. Draft release doc (untracked) used as the operator summary and cross-checked against origin/dev. Drift found and reported, not fixed (other repo): the docs page pins v0.9.5-rc4 while dev's trinity-do-create.sh defaults to v0.9.5-rc5 (rc5 carries the #2862 Caddyfile 0600 fix for the 1-Click image). No .env in ops-public, so no scripts/run.sh use. Final wording assumes v0.9.5 is released (user instruction at commit time): tag guidance says use v0.9.5 / never an -rc tag; rc4/rc5 kept only as the #2862 pre-release 1-Click image history.",
  "changes_applied": [
    "CLAUDE.md Update Trinity v0.9.5 notes: item 2 rebuild list + ent#615 helper / #2742 lock-free status / #2850 allowed-tools; item 4 Alembic 0039→0063 single head; new items 9 (ent#615 git remote token scrub, automatic; sweep log line + ent615 alerts; rotate platform token, mandatory on gitmodules_hits; skills-library clones held the PAT), 10 (PUT operator-resume human-only #2860), 11 (WORKSPACE_ENABLED retired #2836), 12 (re-login/reconnect MCP; #2419 first-sweep critical headroom items)",
    "CLAUDE.md API Access: audit-log note for denied MCP calls (#2807); operator-resume PUT human-only; fan-out #2524 semantics (rows-backed batch, async_mode, deadline bounds the wait → running); interactive model validation (#2796, prefix families, 422, blank = default)",
    "CLAUDE.md Database Operations: new columns (fan_out_task_id 0062, git_dir_bytes BIGINT 0063, room turns stamp source_channel_chat_id under channel `room` #2792); Alembic head 0063 (also in Database Backend)",
    "CLAUDE.md Provisioning: DigitalOcean row adds the one-command size/cost; shortcut paragraph replaced by a 'DigitalOcean one-command install' subsection — prerequisites (Write doctl token, claude setup-token sk-ant-oat01, password rules), tag guidance (release tag e.g. v0.9.5, never -rc), prompts + defaults, what the user-data does (POST /api/token, POST /api/subscriptions, PUT /api/subscriptions/agents/{name}), ~6 min / 15 min timeout / install log, Secure this instance, delete = all data, ops-agent .env, upgrade path, custom domain (DNS-only on Cloudflare, waiting-for-first-visit latch, 422 on bad URL, immediate webhook re-point), hardening summary (tunnel / Tailscale / both, PRIVATE_NETWORK_CIDRS + --caddy-only, what breaks when 80/443 close); --provision paragraph gains --caddy-only + #2862",
    "CLAUDE.md Database Backend: SQLite EOL block rewritten in the past tense (EOL passed 2026-09-01; 0.9.5 still runs on SQLite)",
    "CLAUDE.md Security Notes: machine-credentials subsection gains the ent#628 MCP agent-edge gate and the #2807 refusal audit; new subsection 'Git remotes carry no token (ent#615)' (helper path/registration, .env-first, skills-library + template_service producers, never-strip-unreplaced, rescue file, not-closed items, git credential fill warning); 'Provisioned hosts' retitled + tls-allowed latch guard + 'Private network (VPN-only) access' paragraph (PRIVATE_NETWORK_CIDRS semantics, --caddy-only, validation, container ports dropped over the tailnet)",
    "CLAUDE.md Troubleshooting: hosted/provisioned list gains trinity-do-create timeout + pre-flight errors, rc4 1-Click Caddyfile 0600, tailnet UI unreachable, Telegram/WhatsApp after Public URL save + waiting-for-first-visit, --caddy-only refusals; stale-image list gains #2850 'No description available'; subscription entry gains #2419 inversion, #2789 300s post-switch kill, #2695/#2696 mic + stt_last_failure, #2796 model 422; git-sync entry gains the #2742 leader lease / 180s / SYNC_HEALTH_POLL_INTERVAL_SECONDS / index.lock / #2800 PG overflow paragraph; new entry 'Git fetch/push fails, or an ent#615 token-scrub alert after upgrade' (verification commands, report-field table with alert ids, helper_ok, old-image recreate); sentinel entry notes ent615 alerts are filed under the agent's own name",
    "CLAUDE.md Env table: WORKSPACE_ENABLED marked retired; new rows PRIVATE_NETWORK_CIDRS (start.sh-only, --caddy-only) and SYNC_HEALTH_POLL_INTERVAL_SECONDS (all three compose files, needs compose up -d, ALERT_THRESHOLD 3 → ~180s); Scripts Reference update.sh row",
    "provision/digitalocean.md: one-command section rewritten from the docs page + script — prerequisites table, tag guidance, prompts, what happens (local + first boot + polling), post-install, custom domain, hardening (tunnel / Tailscale commands incl. PRIVATE_NETWORK_CIDRS + --caddy-only / both / close ports), troubleshooting table, delete",
    "scripts/update.sh: verify block waits (≤30s) for ent#615 sweep reports, flags agents with refused / root_readable 0 / tracked .gitmodules, and prints the platform-token rotation reminder when any remote was rewritten (parsing tested against sample log lines)",
    "SKILL provision: DO pre-flight checklist (Write token, sk-ant-oat01, password rules, $48), tag advice (no rc4 1-Click), timeout/log, post-install walkthrough, never run the script for the user",
    "SKILL update: 8b rebuild list additions; new 8d ent#615 verification + rotation + 0.9.5 reminders (WORKSPACE_ENABLED, re-login, #2419 sweep)",
    "SKILL diagnose: 5b adds credential-in-remote scan (http(s) userinfo only, prints agent names), ent#615 sweep-report filter, sync-health / PG int overflow grep; summary line extended",
    "SKILL rollback: rolling back across ent#615 (helper persists; old-base-image recreate needs GITHUB_PAT in .env)",
    "SKILL sync-ops-knowledge: Step 2 notes — read origin/<branch> not the working tree (release-branch checkouts with draft release docs), external docs-site source in trinity-docs + docs/user-docs/guides/deploying, report drift rather than fix it"
  ],
  "changes_skipped": [
    "Workspace UX: Work card activity line (ent#620 #2844), theme switch (ent#625 #2841), room stop / attachments / work-card scoping (#2795/#2794/#2792 beyond the column note), mic capability UI (#2695 beyond the troubleshooting line)",
    "Onboarding UI: checklist header launcher (#2854), checklist refresh (#2862 frontend half), first-run copy (#2848) — no operator lever; hardening-guide content captured instead",
    "#1028 module split (settings → routers/settings/ package, git_service → package, agent_client → package): mounted API byte-identical; no runbook path referenced the moved modules (db/engine.py, mcp_validator unchanged)",
    "#2791 platform/Workspace session unification (frontend-only; covered by the 're-login after upgrade' note), #2761 MCP schedule validation_* params (MCP-layer parity, no ops surface), 1d9022e7 get_agent_assignments fenced (was already documented as skipped/inert)",
    "CI / tests / docs-process commits (#2837 #2833 #2832 #2825 #2816 #2819 #2809 #2804 #2861 #2803), enterprise submodule pin bump (4e1636a9), rc4/rc5 version bumps beyond the tag guidance",
    "Manual DigitalOcean section of provision/digitalocean.md left as-is (its firewall opens 8000/8180 and MCP_PORT=8180 is non-default — pre-existing, outside this range)"
  ],
  "reviewed_unchanged": [
    "Compose files: only WORKSPACE_ENABLED removal and SYNC_HEALTH_POLL_INTERVAL_SECONDS addition in range; no service, port or profile change",
    "scripts/deploy: start.sh (--caddy-only, PRIVATE_NETWORK_CIDRS, Caddyfile validate + 0644) and trinity-do-create.sh (tag rc5) are the only changes; install.sh untouched",
    "Endpoints the runbook cites still present after the settings package split (verified: /token and /api/token both mounted; operator-resume PUT gains reject_agent_principal; fan-out status route; /api/settings/elevenlabs carries stt_last_failure)"
  ]
}
