{
  "last_commit": "2cd064e492ee15faaac1d0c43a1717979a11f7b4",
  "last_sync_date": "2026-07-08",
  "instance_used": "local (~/Dropbox/trinity/trinity on the dev branch)",
  "baseline_before": "c6a2be5e2ccb86e6456a29bcf20195bf719b610f (main, v0.7.0 release)",
  "commits_reviewed": 205,
  "note": "Synced FROM DEV (user arg 'from dev'). dev has diverged from the v0.7.0 baseline (not an ancestor; merge-base a88f5fc8). Reviewed the ops-relevant delta c6a2be5..dev-HEAD. ~/trinity is still a stale April dev checkout (#554) and is NOT the canonical source — use ~/Dropbox/trinity/trinity. .env is absent in ops-public, so scripts/run.sh was not used; diffs were read directly from the local checkout.",
  "changes_applied": [
    "Env table (server): added ELEVENLABS_API_KEY/MODEL_ID/TTS_MAX_CHARS (#24/#25 outbound voice replies), REPORT_RATE_LIMIT (#918), WEBHOOK_RATE_LIMIT/IP_RATE_LIMIT/MAX_BODY_BYTES (#1023/#1424), REDELIVERY_GOVERNOR_ENABLED + 8 tuning vars (#1085), CREDENTIAL_ENCRYPTION_KEY_SECONDARY (#267), VITE_BUG_REPORTING_ENABLED/VITE_BUG_INTAKE_URL (#1116/#1489 build-time)",
    "Docker socket GID CORRECTION (#1131): fixed the now-wrong 'Docker Desktop ignores group_add' claim in 4 places — Non-root #874 security note, 'Backend can't reach Docker socket' troubleshooting (heading de-Linux'd + container-probe command), DOCKER_GID env row, and the start.sh auto-gen sentence. Docker Desktop/Colima/Rancher/rootless present the socket as GID 0; start.sh now probes a throwaway container's socket GID",
    "Service Architecture: added trinity-logs-init (#1478) one-shot alpine init that chowns /data/logs to 1000; backend depends_on it (service_completed_successfully)",
    "Database Operations Key tables: added agent_reports (#918)",
    "API Access: added Agent Reports endpoints (#918 — /api/agents/{name}/reports, /api/reports, /api/reports/stats) and public webhook trigger (POST /api/webhooks/{token}, #291/#1023/#1424)",
    "Security Notes: NEW 'Credential Key Rotation (Issue #267)' subsection — rotate-credential-key.py --apply + CREDENTIAL_ENCRYPTION_KEY_SECONDARY two-key window, docs/migrations/CREDENTIAL_KEY_ROTATION.md"
  ],
  "changes_skipped": [
    "SSO/OIDC (#1303/#1447) + docker-compose.prod.enterprise.yml: live in the closed-source 'enterprise' git submodule (src/backend/enterprise). ops-public manages OSS Trinity — out of scope",
    "Brain Orb (#58, routers/agent_brain_orb.py), Portal chat UI (#78), grid dashboard (#47), per-route titles/favicon (#1416/#1418), fork-to-own (#93), template-catalog curation (#1513), playbooks edit (#959), tasks-fill-height (#1500): frontend/UX, no ops surface",
    "New agent-facing MCP tools report (#918), list_playbooks/run_playbook (#959): per existing convention agent-facing tools are not added to ops CLAUDE.md (the operator-facing HTTP endpoints were documented instead)",
    "WhatsApp outbound media (#1315): messaging-adapter feature, no ops config",
    "#1068 per-task timeout_seconds: unchanged from baseline (still deprecated-but-clamped, commit 762d7344 already in v0.7.0) — no edit"
  ],
  "reviewed_unchanged": [
    "MCP config validator allowlist + protected-files list — unchanged",
    "Redis ACL / two-password model (#589) — unchanged",
    "Only ONE new DB table since baseline (agent_reports); webhooks/playbooks add no tables",
    "Trust model & prompt-injection boundary (#1523) — already present in working tree from a prior edit"
  ]
}
